{
  "$schema": "http://json-schema.org/schema",
  "id": "testSchematics",
  "title": "Test Option Schema",
  "description": "First schematics, generate a file",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Who do you want to greet?"
    }
  },
  "required": [
    "name"
  ]
}
